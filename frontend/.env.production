# URL del backend en Railway. Vite la hornea en el build de producción,
# así el frontend de Vercel llama directo al backend (no a /api de Vercel).
VITE_API_URL=https://lector-tiktok-app-production.up.railway.app
